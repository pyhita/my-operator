apiVersion: mygroup.pyhita.com/v1
kind: ConfigMapSync
metadata:
  labels:
    app.kubernetes.io/name: my-operator
    app.kubernetes.io/managed-by: kustomize
  name: configmapsync-sample
spec:
  # TODO(user): Add fields here
